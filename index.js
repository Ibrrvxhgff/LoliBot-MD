console.log('Iniciando 🚀🚀🚀') 
import cfonts from 'cfonts';
import chalk from 'chalk';

cfonts.say('pain bot', {
  font: 'chrome',
  align: 'center',
  gradient: ['red', 'magenta'],
  transition: false
});

cfonts.say('by: fete', {
  font: 'console',
  align: 'center',
  gradient: ['red', 'magenta'],
  transition: false
});

//console.clear();

import('./main.js');
